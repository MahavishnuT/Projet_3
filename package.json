{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "sass": "sass --watch ./test.scss:./test.css",
    "prefix": "postcss ./public/css/style.css --use autoprefixer -d ./public/css/prefixed/"
  },
  "author": "TomP",
  "license": "ISC",
  "browserlist": "last 4 versions",
  "dependencies": {
    "node-sass": "^7.0.1",
    "postcss-cli": "^9.1.0"
  }
}
